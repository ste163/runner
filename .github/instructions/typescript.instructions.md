---
applyTo: 'src/**/*.{ts,tsx}'
---

Invoke the `/coding-standards` skill for TypeScript style rules (arrow functions, SRP, non-mutability, no lint suppression).

Invoke the `/reactlynx-best-practices` skill for dual-thread rules (background thread restrictions, main thread restrictions, event handler patterns).
