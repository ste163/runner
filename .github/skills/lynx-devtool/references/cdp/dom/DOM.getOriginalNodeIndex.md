# DOM.getOriginalNodeIndex

- `DOM.getOriginalNodeIndex` - Get original node index
- Input: `{nodeId: NodeId}`
- Output: `{result: {index: number}}`
- Description: Returns the original index of the node in its parent
