# DOM.performSearch

- `DOM.performSearch` - Perform search
- Input: `{query: string, includeUserAgentShadowDOM?: boolean}`
- Output: `{result: {searchId: number, resultCount: number}}`
- Description: Searches for nodes in the DOM tree
