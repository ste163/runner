{
  "version": 1,
  "hooks": {
    "agentStop": [
      {
        "type": "command",
        "bash": "echo '--- Verification ---' && bun typecheck && bun lint && bun run verify-docs && echo '✓ typecheck + lint + doc-sync passed'",
        "powershell": "Write-Host '--- Verification ---'; bun typecheck; if ($LASTEXITCODE -eq 0) { bun lint }; if ($LASTEXITCODE -eq 0) { bun run verify-docs }; if ($LASTEXITCODE -eq 0) { Write-Host '✓ typecheck + lint + doc-sync passed' }",
        "cwd": ".",
        "timeoutSec": 60
      }
    ]
  }
}
